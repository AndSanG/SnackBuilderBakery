import { Module } from '@nestjs/common';

/**
 * Menu Management feature module.
 * Domain, application, infrastructure, and presentation layers are added
 * under this folder as each use case is driven by tests.
 */
@Module({})
export class MenuModule {}
